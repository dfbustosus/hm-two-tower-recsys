---
description: Review OpenCode model/provider policy without hardcoding collaborator-specific credentials
agent: research-scout
subtask: true
---

Review model/provider selection for OpenCode in this repository.

Distinguish:

- OpenCode coding/reasoning models, which should usually live in global user config.
- Recommender-system models implemented by the project code.

Check current official OpenCode docs or local `opencode models` output before naming concrete model IDs. Recommend a portable project policy and, if requested, a user-specific global config snippet.
